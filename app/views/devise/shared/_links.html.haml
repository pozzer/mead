%hr
.center-align
  - if controller_name != 'sessions'
    .input-field.col.s12.m12.l12
      %p.margin.medium-small.center-align
        = link_to "Já tenho uma conta", new_session_path(resource_name)
  - if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
    .input-field.col.s12.m12.l12
      %p.margin.medium-small.center-align
        = link_to "Esqueci minha senha", new_password_path(resource_name)
  - if devise_mapping.registerable? && controller_name != 'registrations'
    .input-field.col.s12.m12.l12
      %p.margin.medium-small.center-align
        = link_to "Criar uma conta", new_registration_path(resource_name)
  - if devise_mapping.confirmable? && controller_name != 'confirmations'
    .input-field.col.s12.m12.l12
      %p.margin.medium-small.center-align
        = link_to "Didn't receive confirmation instructions?", new_confirmation_path(resource_name)
  - if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
    .input-field.col.s12.m12.l12
      %p.margin.medium-small.center-align
        = link_to "Didn't receive unlock instructions?", new_unlock_path(resource_name)
  - if devise_mapping.omniauthable?
    .input-field.col.s12.m12.l12
      %p.margin.medium-small.center-align
        - resource_class.omniauth_providers.each do |provider|
          = link_to "Logar com o #{provider.to_s.titleize}", omniauth_authorize_path(resource_name, provider)
