!!!
/
  [if IE 9] <html class="ie9 no-focus">
/
  [if gt IE 9]><!
%html.no-focus
  /
    <![endif]
  = render "layouts/html_head"
  %body
    #page-container.sidebar-l.sidebar-o.side-scroll.header-navbar-fixed
      / Side Overlay
      = render "shared/application/aside"
      / Sidebar
      = render "shared/application/sidebar"
      / END Sidebar
      / Header
      = render "shared/application/header"
      / END Header
      / Main Container
      %main#main-container
        .content.content-narrow
          = render 'shared/alerts'
          = render "shared/content_header", title: get_header_title
          = yield
      / END Main Container
      / Footer
      = render "shared/application/footer"
      / END Footer

