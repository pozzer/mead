- if activity.trackable
  %li
    .list-timeline-time= get_time_ago(activity.created_at)
    %i.si.si-user-follow.list-timeline-icon.bg-success
    .list-timeline-content
      %p
        %b= link_to activity.owner.full_name, profile_path(activity.owner.profile)
        começou a amizade com
        %b= link_to activity.trackable.friend.full_name, profile_path(activity.trackable.friend)
      %ul.nav-users.push-10-t.push
        %li
          %a{:href => profile_path(activity.trackable.friend)}
            = image_tag activity.trackable.friend.profile.avatar_url, class: "img-avatar"
            %i.fa.fa-circle{ :class => "#{get_color_online(activity.trackable.friend)}"}
            = activity.trackable.friend.full_name
            .font-w400.text-muted
              %small=  activity.trackable.friend.profile.organization_name

